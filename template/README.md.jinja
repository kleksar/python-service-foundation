# {{ project_name }}

{{ project_name }} is a Python service created from the Python Service Foundation Copier template.

## Local development

Create an isolated environment, install the service dependencies, and keep configuration in a local `.env` file that is never committed.

## Project identifier

`{{ project_slug }}`
